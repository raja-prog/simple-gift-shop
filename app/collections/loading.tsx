// Centered cute animated gift shown while the collections data loads.
import { GiftLoader } from "@/components/GiftLoader";

export default function CollectionsLoading() {
  return <GiftLoader label="Wrapping up the collections…" />;
}
